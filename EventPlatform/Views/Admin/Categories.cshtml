@{
    ViewData["Title"] = "Manage Categories";
}

<!-- === TAB: QUẢN LÝ CATEGORY === -->
<section id="manage-categories" class="content-section">
    <div class="card">
        <div class="card-header"><h3>Manage Event Categories</h3></div>
        <p>Phần này bạn có thể thêm form để tạo mới, và bảng để sửa/xóa các Category (lấy từ bảng `EventCategories` trong ERD).</p>
        <!-- Thêm UI CRUD (Create, Read, Update, Delete) cho Categories ở đây -->
    </div>
</section>